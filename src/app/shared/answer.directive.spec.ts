import { AnswerDirective } from './answer.directive';

describe('AnswerDirective', () => {
  it('should create an instance', () => {
    const directive = new AnswerDirective();
    expect(directive).toBeTruthy();
  });
});
